# Core agent runtime
openai-agents>=0.0.19
openai>=1.60.0
pydantic>=2.7

# Spreadsheet / document handling used by the agents and the evaluators
openpyxl>=3.1

# HTTP stack (pinned indirectly by the OpenAI SDK; listed for reproducibility)
httpx>=0.27
httpcore>=1.0

# Config parsing for skill front-matter
PyYAML>=6.0

# Optional: execution-structure diagrams (`--viz`). The pipeline runs without it.
graphviz>=0.20
